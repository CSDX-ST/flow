// components/Initial/initialEdges.ts
import {MarkerType} from "@xyflow/react";
const initialEdges = [

    // { id: 'e1-2', source: '1', sourceHandle:'a1',target: '2' ,type:'buttonedge'},

];

export default initialEdges;